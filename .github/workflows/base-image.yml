on:
  push:
    branches:
      - "rebase-*"

jobs:
  build_base_image:
    runs-on: linux-amd64-cpu16
    uses: ./.github/workflows/base-workflow.yml
    with:
      tag: maxtext_base
      stage: maxtext_base

